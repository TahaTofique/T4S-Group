import {
  ShieldCheck,
  Building2,
  Home,
  Factory,
  PartyPopper,
  UserCheck,
  HardHat,
  Hammer,
  ClipboardList,
  KeyRound,
  type LucideIcon,
} from 'lucide-react'

/**
 * Maps the `icon` string stored in companyData.ts to an actual Lucide
 * component. Add new entries here if you introduce a new service icon.
 */
export const iconMap: Record<string, LucideIcon> = {
  ShieldCheck,
  Building2,
  Home,
  Factory,
  PartyPopper,
  UserCheck,
  HardHat,
  Hammer,
  ClipboardList,
  KeyRound,
}
